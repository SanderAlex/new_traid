<p class="seo">Admin</p>

<table class="admin">
<tr>
<th>Name</th>
<th>Section</th>
<th>Subsection</th>
<th></th>
<th></th>
<th></th>
</tr>
<% @products.each do |product| %>
<tr>
<td><%= product.name %></td>
<td><%= product.section %></td>
<td><%= product.subsection %></td>
<td><%= link_to 'Show', product %></td>
<td><%= link_to 'Edit', edit_product_path(product) %></td>
<td><%= link_to 'Destroy', product, :confirm => 'Are you sure?',
:method => :delete %></td>
</tr>
<% end %>
<th><%= link_to 'New product', new_path %></th>
</table>

